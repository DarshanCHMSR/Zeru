import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cross-Chain Gas Tracker | Real-Time Gas Price Monitoring",
  description: "Real-time cross-chain gas price tracker with wallet simulation. Monitor Ethereum, Polygon, and Arbitrum gas prices with interactive candlestick charts and transaction cost simulation.",
  keywords: "ethereum, polygon, arbitrum, gas prices, blockchain, web3, transaction costs, crypto",
  authors: [{ name: "Zeru Gas Tracker" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
